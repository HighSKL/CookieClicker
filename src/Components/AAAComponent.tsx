import React, { FC } from 'react';
import withModalWindow from '../Assets/Common/ModalWindow';

const AAAComponent: FC = () => {
    return (
        <div>
            what`s
        </div>
    );
};

export default withModalWindow(AAAComponent);